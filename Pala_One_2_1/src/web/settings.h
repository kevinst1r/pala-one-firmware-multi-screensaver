#ifndef PALA_WEB_SETTINGS_H
#define PALA_WEB_SETTINGS_H

// Mounts /settings (GET + POST) and /del-sleep (POST — deletes custom
// screensaver image).
void registerSettingsRoutes();

#endif  // PALA_WEB_SETTINGS_H
